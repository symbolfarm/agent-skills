repos:
  - repo: local
    hooks:
      - id: public-boundary-denylist
        name: keep private examples out of public skills
        entry: python3 scripts/check-public-boundary.py --staged
        language: system
        pass_filenames: false
        always_run: true
